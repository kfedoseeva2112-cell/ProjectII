def get_recommendations(features, occasion):
    rules = load_rules()
    # st.write("Количество загруженных правил:", len(rules))  # раскомментировать для отладки
    if not rules:
        return {
            "одежда": ["Нет правил", "Проверьте файл"],
            "цвета": [],
            "аксессуары": [],
            "makeup": [],
            "style_tip": "Ошибка загрузки базы правил."
        }
   
